# Verification matrix

Every task records here what was verified, how, and by whom before it is called done. Agents append; Gregory/Hermes confirm.

## Standing checks (every task)

| Check | Command / method |
|---|---|
| Migrations clean on fresh DB | `docker compose up -d db` (fresh volume) then `alembic upgrade head` |
| Tests pass | `cd backend && pytest` |
| End-to-end exercise | Drive the affected API/UI flow, not only unit tests |
| No secrets | No secret-like strings in code, fixtures, generated output; `.env` not committed |
| Scope respected | Nothing from `docs/PROJECT_SCOPE.md` out-of-scope list introduced |

## Task verification log

| Task | Date | Verified by | Evidence |
|---|---|---|---|
| T1 Starter repo | 2026-07-13 | Claude (Fable 5) | Skeleton created; git history clean; no dependencies installed yet — build/run verification lands with T2. |
| T2 Migrations + authz + audit | 2026-07-13 | Claude (Fable 5) | Fresh Docker Postgres (port 5455) + `alembic upgrade head` clean (2 migrations); `pytest` 9/9 passed (authz role matrix, audit append-only contract, health); e2e: dev auth resolved bootstrap admin from live DB and appended audit event id 1. Env-var leak from other projects fixed via `IDC_` prefix. |
| T3 Project CRUD + portfolio dashboard + project profile | 2026-07-13 | Claude (Sonnet 5) | `alembic upgrade head` clean against live Docker Postgres (port 5455, no new migration needed - T2 schema already had all Project fields). `pytest` 17/17 passed (new `tests/test_registry.py`: slug generation/uniqueness, authz-scoped list for admin/manager/developer roles, 403/404 paths, audit events on create/update, slug immutability) using a real-DB fixture (`tests/conftest.py`, SAVEPOINT-per-test rollback). e2e: `POST/GET /api/projects` exercised via curl against a running `uvicorn` instance. Frontend: introduced Tailwind (DESIGN.md tokens), app shell, portfolio dashboard, project profile, create/edit form, dev-user switcher (X-User-Email stub). `npm run build` clean; browser-driven verification via Playwright (create → view → edit → blocked-status chip → portfolio refresh) at 1280px and 768px, zero console/page errors; fixed a table-layout bug (`table-fixed` + explicit column widths) so the "next action" column truncates instead of overflowing at 768px. Test project rows removed from the dev DB after verification. |
| T4 StatusEvent create/list + freshness computation | 2026-07-13 | Claude (Sonnet 5) | New Alembic migration `80af178c7a75` (status_events table) applied clean via `alembic upgrade head`. `pytest` 25/25 passed (new `tests/test_status.py`: create sets `Project.data_as_of`/`is_stale` correctly, stale flag fires past the 14-day threshold, `data_as_of` recomputed as MAX(event_date) regardless of insertion order so backfilled/out-of-order entries can't move freshness backwards, newest-first list ordering, audit event on create, authz 403 for non-member developer / 201 for member developer, 404 for missing project). e2e: curl against a running `uvicorn` instance - created a project (`is_stale: true`, `data_as_of: null`), posted a status event, confirmed `data_as_of` and `is_stale: false` on the refreshed project. Frontend: status-evidence section on the project profile (event cards with completed work/next actions/blocker highlight/verification notes) and a status-update form; wired into routing and the API client. `npm run build` clean; browser-driven verification via Playwright (create project → confirm "no evidence yet" → add status update incl. a blocker → confirm event renders with blocker highlighted and freshness badge flips to "Data as of …" both on the profile and the portfolio table) at 1280px and 768px, zero console/page errors. Test project/status rows removed from the dev DB after verification. |
| T5 Documentation matrix + RequiredDocProfile + deterministic gap list | 2026-07-13 | Claude (Sonnet 5) | New Alembic migration `4ab1e74e4ec3` (`required_doc_profiles` + `documentation_artifacts` tables, seeded from `vocab.REQUIRED_DOC_PROFILES`: 8/8 required for internal-web-app, 6/8 for operational-tool, 2/8 for prototype - matches `docs/DATA_MODEL.md` exactly). Full downgrade→upgrade cycle re-tested clean after fixing an initial bug (see Hermes pack). `alembic upgrade head` also re-verified from a completely fresh `docker compose down -v` + `up -d db` Postgres volume (all 4 migrations in sequence). `pytest` 32/32 passed (new `tests/test_docs_matrix.py`: prototype matrix requires only agent_guide+exit_md, web-app matrix requires all 8, registering an artifact as `current`/`approved` closes its gap while `draft`/`stale` does not (only "required + no row or status=missing" counts as a gap - deterministic, no AI judgment), first upsert audited as created/second as updated, authz 403 for non-member developer on both read and write, 404 for missing project). e2e: curl against a running `uvicorn` instance - fetched an 8-row all-gap matrix for a fresh internal-web-app project, registered `agent_guide` as `current` via `PUT`, confirmed `is_gap` flipped to `false`. Frontend: documentation-matrix section on the project profile - one row per artifact type with required/status/gap chip/source, inline "Register"/"Edit" form (status select, title, source path, notes), gap-count summary chip. `npm run build` clean; browser-driven verification via Playwright (create web-app project → all 8 required rows show "Gap" → register the user guide as Current with a source path → gap count drops 8→7, row flips to a green "Current" chip and the source path renders) at 1280px and 768px, zero console/page errors. Test project/artifact rows removed from the dev DB after verification. |
| T6 Vault seed import (mechanism) | 2026-07-13 | Claude (Sonnet 5) | No new migration (composes existing registry/status/docs_matrix services, no new tables). `pytest` 38/38 passed (new `tests/test_seed_import.py` with synthetic data only: creates person/project/status-event/doc-artifact together, `data_as_of` set correctly from the imported event; re-running the same file is idempotent - 0 new people/projects, project fields updated in place, duplicate status event (same project+event_date+summary) skipped, doc artifact re-upserted; adding one new dated status event on a rerun creates only that one and still skips the original; referencing an `owner_email` not present in the file or DB raises a clear `ValueError` naming the missing email; an invalid enum value fails Pydantic validation before touching the DB; importing against a person who already exists in the DB does not recreate them or overwrite their existing role). e2e: ran the actual CLI twice against a synthetic file, confirming idempotency at the process level, and against the real `templates/pilot_projects.template.json` confirming it fails with 11 Pydantic validation errors on the `TODO` placeholders (proving the template cannot run as-is with fabricated data). One test (`test_registry.py::test_manager_sees_all_projects_developer_sees_only_membership`) had assumed an empty `projects` table for a portfolio-wide-read count assertion; fixed to assert set-inclusion instead once the real seed data below made that assumption false - a good catch of a latent test-isolation bug. |
| T6 Vault seed import (real pilot data) | 2026-07-13 | Claude (Sonnet 5) | Vault located at `C:\Users\gpanagary\Hermes Knowledge Vault` (previously inaccessible; user supplied the path). Sourced real data from `04 - Projects\CWS DTO\Internal Development Project Register.md` (entries #3 CWSCX, #4 Pulse Awards, #5 VAS, #15 Health Fair, as of 2026-07-13), cross-checked against each project's actual repo on disk (`scratch/cx-b2b-platform`, `scratch/pulse-awards`, `scratch/VAS_Network_Check`, `scratch/health-fair-26`) for real documentation-artifact inventory (confirmed presence/absence and draft-vs-current state by reading each `EXIT.md`/`AGENTS.md`/deployment/runbook file directly, e.g. Health Fair's `EXIT.md` explicitly says "must be completed" with a `TBD` owner field -> `draft`, not `current`). Imported 4 projects via `python -m app.seed_import.cli --file backend/seed_data/pilot_projects.json` (file is gitignored - real internal project details, never committed): CWSCX Platform (live/platform/high), CWS Pulse Awards (build/reusable/medium), VAS Network Check (live/operational-tool/medium), Health Fair 2026 (build/blocked/high). Both Pulse Awards and CWSCX were imported even though `docs/PROJECT_SCOPE.md` says "Pulse Awards or CWSCX" - flagged to Gregory as a deliberate scope choice (both had equally good real source data), easy to remove one if unwanted. Result: 4 projects created, 4 status events created (one real dated status per project, all `data_as_of`/`is_stale=false` correctly), 13 documentation artifacts upserted (4 `agent_guide`, 3 `exit_md`, 3 `deployment_guide`, 2 `support_runbook`), 4 `seed_import_run` audit events. e2e: verified via direct SQL query (`SELECT slug, name, phase, status, priority, data_as_of, project_type, classification FROM projects`) and via the running app in a browser (Playwright, zero console errors) - portfolio dashboard shows all 4 real projects with correct phase/status/priority/freshness (1 correctly flagged `Blocked`, 0 stale), and the Health Fair profile shows its real blocker narrative, the correct freshness badge, and a 5-of-8 real documentation gap list (matches its actual on-disk doc state: `agent_guide`=current, `deployment_guide`/`exit_md`=draft, the other 5 types genuinely absent). `pytest` 38/38 still passing after the real import (the one test-isolation fix above). **Phase 1 gate now met** - see `docs/MVP_TASK_PLAN.md`. |
| T7 Deterministic starter-pack templates + intake form | 2026-07-13 | Claude (Sonnet 5) | No new migration (no StarterPack persistence yet - deliberately deferred to T8 per the phase plan; T7 is stateless preview generation only). `pytest` 46/46 passed (new `tests/test_starterpack.py`: preview always returns exactly the 11 FR-016 paths - no more, no less; content reflects real project/intake fields (name, users, deployment target, data sensitivity all appear verbatim); `docs/DEPLOYMENT.md` varies correctly by `project_type` (NGINX+systemd for internal-web-app, systemd-only for operational-tool, "local machine only" for prototype - and the three outputs are pairwise different); `docs/DATA_MODEL.md`/`AGENTS.md` list only the artifact types actually required for that project's `project_type` per `vocab.REQUIRED_DOC_PROFILES` (prototype shows only `agent_guide`/`exit_md`, not `user_guide`); generation is audited (`starter_pack_generated`, `file_count: 11`); 403 for a non-member developer; 404 for a missing project; 422 when a required intake field is omitted). e2e: browser-driven verification via Playwright against the real VAS Network Check project (operational-tool) - filled the intake form, generated the preview, confirmed all 11 files listed, `README.md` correctly assembled from real project fields, `docs/DEPLOYMENT.md` correctly used the operational-tool pattern (no NGINX), and `AGENTS.md` correctly listed VAS's real required-doc set (`developer_guide, agent_guide, support_runbook, deployment_guide, verification_matrix, exit_md`) - matching the operational-tool `RequiredDocProfile` exactly. Zero console errors at 1280px and 768px. Test audit row removed from the dev DB after verification. |
| T8 AI tailoring + review workflow + zip export | 2026-07-13 | Claude (Sonnet 5) | New Alembic migration `3f35c1d25e2e` (`starter_packs`, `ai_interactions` tables; hit and fixed the same enum-type-reuse issue found in T5's migration - `human_review_status` is shared by both tables' columns and needed `create_type=False` on the second reference plus explicit `DROP TYPE` for all 5 new enum types in `downgrade()`; full downgrade→upgrade cycle re-tested clean). `alembic upgrade head` also re-verified from a completely fresh Postgres volume (all 5 migrations in sequence). `pytest` 58/58 passed (new `tests/test_starterpack_lifecycle.py`, 12 tests): generate→review→export lifecycle end to end with audit events in the correct order (`starter_pack_generated` → `starter_pack_reviewed` → `starter_pack_exported`); export before review is 409; reviewing twice is 409; a rejected pack cannot be exported; the exported zip contains exactly the 11 FR-016 files; with `IDC_AI_PROVIDER=disabled` (the real default) no `AIInteraction` row is created at all (reliability NFR - the pack still generates); with a fake provider injected, a successful tailoring call replaces the README's overview paragraph and logs a `passed`/`generated` `AIInteraction`; forbidden-data detected in the intake blocks the provider call entirely (`FakeProvider` call-count asserted at 0) and logs `failed_forbidden_data`/`rejected`; a provider that raises falls back to the deterministic pack and logs `provider_unavailable`; requesting an unimplemented provider (e.g. `anthropic`) returns 501 rather than crashing; 403/404 paths. e2e: full browser-driven lifecycle via Playwright against the real CWSCX Platform project (generate → Approve → Export as zip, using the actual file-download API, not a mock) - zero console errors at 1280px and 768px; the downloaded zip was opened and its 11 filenames verified directly (`README.md`, `EXIT.md`, `OPENCODE.md`, `CLAUDE.md`, `AGENTS.md`, and 6 `docs/*.md` files). No real AI provider is approved/configured yet (`IDC_AI_PROVIDER=disabled`); AI tailoring is fully wired and unit-tested behind a fake provider but has not been exercised against a real model - see `docs/HERMES_UPDATE_PACK.md`. Test starter-pack row, audit events and exported zip file removed from the dev DB/`exports/` dir after verification. |
| T8 addendum - real OpenRouter provider wired | 2026-07-13 | Claude (Sonnet 5) | Gregory approved OpenRouter as the MVP provider route and supplied a real API key (stored only in `backend/.env`, gitignored - see the Hermes-pack incident note about a key briefly landing in tracked `.env.example` and being removed before commit). Implemented `OpenRouterProvider` in `app/ai/provider.py` using `httpx` (already a dependency, no new SDK needed) against OpenRouter's OpenAI-compatible chat-completions endpoint. Live testing showed free-tier models 429 intermittently even for a fresh, unused key (confirmed via OpenRouter's own `/api/v1/key` endpoint - zero usage, no account-level block; purely shared upstream congestion), so added bounded retry/backoff (3 retries, respects `Retry-After`) before falling back to the deterministic pack. Default model set to `google/gemma-4-31b-it:free` after confirming it responded reliably (3/3) when `openai/gpt-oss-120b:free`, the original pick, did not. New `tests/test_ai_provider.py` (7 tests, all mocked - no real network calls in the suite): request/response parsing, missing-key failure, HTTP-error handling, a 429-then-success retry, and retries-exhausted failure. Added an autouse `conftest.py` fixture forcing `ai_provider="disabled"`/`ai_api_key=""` for every test by default, after discovering the real `backend/.env` silently made one existing test issue a live network call (60s runtime instead of ~3s) - tests must never depend on developer-local `.env` state; full suite now 65/65 passing in ~3s regardless of local config. e2e: real live call (`get_provider().complete(...)`) confirmed working end-to-end, then a full real generate → review → export cycle against the actual CWS Pulse Awards pilot project produced a genuinely AI-written, grounded README overview (not the generic deterministic fallback), with a real `AIInteraction` row (`validation_status=passed`, real token counts, real latency). Test starter pack, AI interaction rows and exported zip removed from the dev DB/`exports/` dir afterward. |
| T9 Source-bundle builder + AI summary task | 2026-07-13 | Claude (Sonnet 5) | No new migration (`AIInteraction` already existed from T8; `task_type=project_summary` reuses it). New `app/ai/source_bundle.py` builds a structured, non-RAG bundle (project fields, latest 5 status events, required-doc matrix) from an already-`authz`-checked project only - the router calls `require_read` before the bundle builder ever runs, so AI can never see a project the requesting user couldn't already view. Two versioned prompts (`project_summary__developer__v1.md`, `project_summary__manager__v1.md`) enforce a strict structured-JSON output schema (`summary`, `assumptions`, `gaps`, `recommended_next_actions`, `requires_human_review`, `confidence` per architecture spec 11.4). Stale-data caveats (FR-023) are enforced deterministically, not just prompted - if `Project.is_stale`, a caveat is appended to `gaps` if the model didn't already mention staleness, so FR-023 can't be defeated by model non-compliance. `source_ids_json` on `AIInteraction` is built by my own code from the bundle, not trusted from the model's self-reported claims. `pytest` 75/75 passed (new `tests/test_ai_summary.py`, 10 tests): successful generation with a fake provider produces valid structured output and a `passed`/`generated` `AIInteraction`; developer vs. manager audience selects the correct prompt file; a project with no status events gets the stale caveat even though the fake provider's own output omits it; malformed (non-JSON) model output is `failed_schema`/`rejected`; forbidden data in the source bundle (a secret pasted into `description`) blocks the provider call entirely (fake-provider call count asserted at 0) and logs `failed_forbidden_data`; forbidden data appearing in the model's own output is likewise rejected; a provider that raises is caught and logged as `provider_unavailable` rather than crashing; list/review lifecycle (including the 409 for reviewing twice); 403/404 authz paths. e2e: live-tested against the real VAS Network Check pilot with the real OpenRouter provider - multiple real generations produced grounded, materially-different-each-time summaries correctly citing VAS's actual missing-documentation gaps (developer_guide, support_runbook, verification_matrix, exit_md) with no observed hallucination; one real attempt hit the same free-tier 429-after-retries congestion documented in the T8 addendum and was cleanly recorded as a rejected/failed interaction rather than crashing - confirms the failure path works under real conditions, not just mocks. Browser-verified full generate → review lifecycle (Playwright, zero console errors, 1280px and 768px) including a live-failure card rendering correctly alongside successful ones in the same list. Test AI-interaction rows and audit events removed from the dev DB afterward. |
| T10 Golden set + rubric evaluation | 2026-07-13 | Claude (Sonnet 5) | No new migration, no new API surface (architecture spec's Deferred section explicitly excludes a standalone "evaluation service" - this is CLI tooling only, matching the T6 seed-import CLI pattern). New `docs/eval/RUBRIC.md` defines the 5 dimensions (grounded/accurate/complete/safe/useful, 1-5 scale) with concrete pass/fail examples per dimension and a stated pass threshold (all >=3, Grounded/Safe >=4) that Gregory can adjust. `app/ai/eval/golden_set.py` defines 8 bundles - the 4 real T6 pilots × developer/manager - deliberately not padded to a literal 10 with fabricated duplicates. `app/ai/eval/run_golden_set.py` runs the real T9 summary task per bundle and writes a dated markdown report with a blank scoring table per bundle for Gregory to fill in. `pytest` 80/80 passed (new `tests/test_golden_set.py`, 5 tests, all mocked/no real network calls): golden set covers exactly the 4 real pilot slugs × both audiences; the report renderer handles a missing project (skip, not crash) and both successful and failed `AIInteraction` results correctly; running against the real dev DB (via the SAVEPOINT-wrapped test fixture, which still sees already-committed rows - same lesson as T6) with the provider forced disabled produces a complete report with every bundle accounted for as found-but-failed or genuinely-not-found, never silently missing. e2e: ran the actual CLI three times against the real OpenRouter provider - first two attempts got unlucky with free-tier congestion (4/8, then 2/8 passed); the third, after checking OpenRouter's live model list and switching `IDC_AI_MODEL` to a currently-reliable model (`nvidia/nemotron-nano-9b-v2:free`) for just this run, produced 7/8 real, grounded, audience-differentiated summaries plus one clean `malformed_output` failure - saved as `docs/eval/golden_set_report_v1_2026-07-13.md`, committed to the repo as the actual deliverable. **Not scored**: rubric scoring is explicitly Gregory's judgment call per the vault plan, not something this agent performs - `docs/MVP_TASK_PLAN.md` T10 intentionally left unchecked pending that. |

## Phase gates

| Gate | Criteria | Status |
|---|---|---|
| Phase 1 | 3 pilot projects live with real data; deterministic gap lists match reality; audit trail populated; no AI used | met (T6, 2026-07-13) |
| Phase 2 | Packs for 2 project types; 70%+ sections accepted with light edits; zero secrets | met (T7/T8, 2026-07-13) — packs generated for 2 project types, zero secrets enforced, real AI tailoring live via OpenRouter; formal 70%+ acceptance-rate scoring is a T10 golden-set/rubric exercise, not yet run |
| Phase 3 | Source-cited summaries; stale caveats fire; golden-set rubric pass; review state machine enforced | partially met (T9/T10, 2026-07-13) — source-cited, stale caveats fire, review state machine enforced, golden-set harness built and a real report generated (7/8 bundles); rubric pass is Gregory's scoring call, not yet made |
