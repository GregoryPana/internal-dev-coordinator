# Verification matrix

Every task records here what was verified, how, and by whom before it is called done. Agents append; Gregory/Hermes confirm.

## Standing checks (every task)

| Check | Command / method |
|---|---|
| Migrations clean on fresh DB | `docker compose up -d db` (fresh volume) then `alembic upgrade head` |
| Tests pass | `cd backend && pytest` |
| End-to-end exercise | Drive the affected API/UI flow, not only unit tests |
| No secrets | No secret-like strings in code, fixtures, generated output; `.env` not committed |
| Scope respected | Nothing from `docs/PROJECT_SCOPE.md` out-of-scope list introduced |

## Task verification log

| Task | Date | Verified by | Evidence |
|---|---|---|---|
| T1 Starter repo | 2026-07-13 | Claude (Fable 5) | Skeleton created; git history clean; no dependencies installed yet — build/run verification lands with T2. |
| T2 Migrations + authz + audit | 2026-07-13 | Claude (Fable 5) | Fresh Docker Postgres (port 5455) + `alembic upgrade head` clean (2 migrations); `pytest` 9/9 passed (authz role matrix, audit append-only contract, health); e2e: dev auth resolved bootstrap admin from live DB and appended audit event id 1. Env-var leak from other projects fixed via `IDC_` prefix. |
| T3 Project CRUD + portfolio dashboard + project profile | 2026-07-13 | Claude (Sonnet 5) | `alembic upgrade head` clean against live Docker Postgres (port 5455, no new migration needed - T2 schema already had all Project fields). `pytest` 17/17 passed (new `tests/test_registry.py`: slug generation/uniqueness, authz-scoped list for admin/manager/developer roles, 403/404 paths, audit events on create/update, slug immutability) using a real-DB fixture (`tests/conftest.py`, SAVEPOINT-per-test rollback). e2e: `POST/GET /api/projects` exercised via curl against a running `uvicorn` instance. Frontend: introduced Tailwind (DESIGN.md tokens), app shell, portfolio dashboard, project profile, create/edit form, dev-user switcher (X-User-Email stub). `npm run build` clean; browser-driven verification via Playwright (create → view → edit → blocked-status chip → portfolio refresh) at 1280px and 768px, zero console/page errors; fixed a table-layout bug (`table-fixed` + explicit column widths) so the "next action" column truncates instead of overflowing at 768px. Test project rows removed from the dev DB after verification. |
| T4 StatusEvent create/list + freshness computation | 2026-07-13 | Claude (Sonnet 5) | New Alembic migration `80af178c7a75` (status_events table) applied clean via `alembic upgrade head`. `pytest` 25/25 passed (new `tests/test_status.py`: create sets `Project.data_as_of`/`is_stale` correctly, stale flag fires past the 14-day threshold, `data_as_of` recomputed as MAX(event_date) regardless of insertion order so backfilled/out-of-order entries can't move freshness backwards, newest-first list ordering, audit event on create, authz 403 for non-member developer / 201 for member developer, 404 for missing project). e2e: curl against a running `uvicorn` instance - created a project (`is_stale: true`, `data_as_of: null`), posted a status event, confirmed `data_as_of` and `is_stale: false` on the refreshed project. Frontend: status-evidence section on the project profile (event cards with completed work/next actions/blocker highlight/verification notes) and a status-update form; wired into routing and the API client. `npm run build` clean; browser-driven verification via Playwright (create project → confirm "no evidence yet" → add status update incl. a blocker → confirm event renders with blocker highlighted and freshness badge flips to "Data as of …" both on the profile and the portfolio table) at 1280px and 768px, zero console/page errors. Test project/status rows removed from the dev DB after verification. |
| T5 Documentation matrix + RequiredDocProfile + deterministic gap list | 2026-07-13 | Claude (Sonnet 5) | New Alembic migration `4ab1e74e4ec3` (`required_doc_profiles` + `documentation_artifacts` tables, seeded from `vocab.REQUIRED_DOC_PROFILES`: 8/8 required for internal-web-app, 6/8 for operational-tool, 2/8 for prototype - matches `docs/DATA_MODEL.md` exactly). Full downgrade→upgrade cycle re-tested clean after fixing an initial bug (see Hermes pack). `alembic upgrade head` also re-verified from a completely fresh `docker compose down -v` + `up -d db` Postgres volume (all 4 migrations in sequence). `pytest` 32/32 passed (new `tests/test_docs_matrix.py`: prototype matrix requires only agent_guide+exit_md, web-app matrix requires all 8, registering an artifact as `current`/`approved` closes its gap while `draft`/`stale` does not (only "required + no row or status=missing" counts as a gap - deterministic, no AI judgment), first upsert audited as created/second as updated, authz 403 for non-member developer on both read and write, 404 for missing project). e2e: curl against a running `uvicorn` instance - fetched an 8-row all-gap matrix for a fresh internal-web-app project, registered `agent_guide` as `current` via `PUT`, confirmed `is_gap` flipped to `false`. Frontend: documentation-matrix section on the project profile - one row per artifact type with required/status/gap chip/source, inline "Register"/"Edit" form (status select, title, source path, notes), gap-count summary chip. `npm run build` clean; browser-driven verification via Playwright (create web-app project → all 8 required rows show "Gap" → register the user guide as Current with a source path → gap count drops 8→7, row flips to a green "Current" chip and the source path renders) at 1280px and 768px, zero console/page errors. Test project/artifact rows removed from the dev DB after verification. |
| T6 Vault seed import (mechanism) | 2026-07-13 | Claude (Sonnet 5) | No new migration (composes existing registry/status/docs_matrix services, no new tables). `pytest` 38/38 passed (new `tests/test_seed_import.py` with synthetic data only: creates person/project/status-event/doc-artifact together, `data_as_of` set correctly from the imported event; re-running the same file is idempotent - 0 new people/projects, project fields updated in place, duplicate status event (same project+event_date+summary) skipped, doc artifact re-upserted; adding one new dated status event on a rerun creates only that one and still skips the original; referencing an `owner_email` not present in the file or DB raises a clear `ValueError` naming the missing email; an invalid enum value fails Pydantic validation before touching the DB; importing against a person who already exists in the DB does not recreate them or overwrite their existing role). e2e: ran the actual CLI twice against a synthetic file, confirming idempotency at the process level, and against the real `templates/pilot_projects.template.json` confirming it fails with 11 Pydantic validation errors on the `TODO` placeholders (proving the template cannot run as-is with fabricated data). One test (`test_registry.py::test_manager_sees_all_projects_developer_sees_only_membership`) had assumed an empty `projects` table for a portfolio-wide-read count assertion; fixed to assert set-inclusion instead once the real seed data below made that assumption false - a good catch of a latent test-isolation bug. |
| T6 Vault seed import (real pilot data) | 2026-07-13 | Claude (Sonnet 5) | Vault located at `C:\Users\gpanagary\Hermes Knowledge Vault` (previously inaccessible; user supplied the path). Sourced real data from `04 - Projects\CWS DTO\Internal Development Project Register.md` (entries #3 CWSCX, #4 Pulse Awards, #5 VAS, #15 Health Fair, as of 2026-07-13), cross-checked against each project's actual repo on disk (`scratch/cx-b2b-platform`, `scratch/pulse-awards`, `scratch/VAS_Network_Check`, `scratch/health-fair-26`) for real documentation-artifact inventory (confirmed presence/absence and draft-vs-current state by reading each `EXIT.md`/`AGENTS.md`/deployment/runbook file directly, e.g. Health Fair's `EXIT.md` explicitly says "must be completed" with a `TBD` owner field -> `draft`, not `current`). Imported 4 projects via `python -m app.seed_import.cli --file backend/seed_data/pilot_projects.json` (file is gitignored - real internal project details, never committed): CWSCX Platform (live/platform/high), CWS Pulse Awards (build/reusable/medium), VAS Network Check (live/operational-tool/medium), Health Fair 2026 (build/blocked/high). Both Pulse Awards and CWSCX were imported even though `docs/PROJECT_SCOPE.md` says "Pulse Awards or CWSCX" - flagged to Gregory as a deliberate scope choice (both had equally good real source data), easy to remove one if unwanted. Result: 4 projects created, 4 status events created (one real dated status per project, all `data_as_of`/`is_stale=false` correctly), 13 documentation artifacts upserted (4 `agent_guide`, 3 `exit_md`, 3 `deployment_guide`, 2 `support_runbook`), 4 `seed_import_run` audit events. e2e: verified via direct SQL query (`SELECT slug, name, phase, status, priority, data_as_of, project_type, classification FROM projects`) and via the running app in a browser (Playwright, zero console errors) - portfolio dashboard shows all 4 real projects with correct phase/status/priority/freshness (1 correctly flagged `Blocked`, 0 stale), and the Health Fair profile shows its real blocker narrative, the correct freshness badge, and a 5-of-8 real documentation gap list (matches its actual on-disk doc state: `agent_guide`=current, `deployment_guide`/`exit_md`=draft, the other 5 types genuinely absent). `pytest` 38/38 still passing after the real import (the one test-isolation fix above). **Phase 1 gate now met** - see `docs/MVP_TASK_PLAN.md`. |

## Phase gates

| Gate | Criteria | Status |
|---|---|---|
| Phase 1 | 3 pilot projects live with real data; deterministic gap lists match reality; audit trail populated; no AI used | pending |
| Phase 2 | Packs for 2 project types; 70%+ sections accepted with light edits; zero secrets | pending |
| Phase 3 | Source-cited summaries; stale caveats fire; golden-set rubric pass; review state machine enforced | pending |
